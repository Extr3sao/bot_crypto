# Reason-code registry

Stable V1 codes include `UNRESOLVED_CONFLICT`, `MAX_POSITIONS`, `DUPLICATE_INTENT`, `RISK_REJECT`, and `OTHER`. Raw reasons remain in `raw_reason`; no information is discarded.
