# Trace non-interference

The trace foundation is sidecar-only and has no runtime wiring. This proves the library cannot alter a decision in its current deployment state; a future runtime adapter requires an on/off equivalence test before activation.
