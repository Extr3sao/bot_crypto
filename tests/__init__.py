"""Suite de tests del bot.

Convención:
- ``tests/unit/`` → pruebas aisladas, sin red ni disco.
- ``tests/integration/`` → pruebas que requieren servicios externos
  (incluye las marcadas con ``@pytest.mark.integration``).
- ``tests/regression/`` → pruebas históricas contra datasets fijos.
"""
